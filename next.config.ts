// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Add any additional config options below
  // Example: experimental: { appDir: true },
};

export default nextConfig;
